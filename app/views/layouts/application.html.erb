<!DOCTYPE html>
<html>
  <head>
    <title>FoodWare</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    


    <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Inicio</a>

      </div>

      <div id="navbar" class="navbar-collapse collapse">
        <form class="navbar-form navbar-right"> 
        <% if ! user_signed_in? %>      
          <div class="form-group">
            <input type="text" placeholder="Email" class="form-control">
          </div>
          <div class="form-group">
            <input type="password" placeholder="Password" class="form-control">
          </div>
          <button type="submit" class="btn btn-success">Sign in</button>
        <% end %>
        <% if user_signed_in? %>
          <p><a class="btn btn-warning" <%= link_to "Log Out", destroy_user_session_path, method: :delete%> </a></p>
        <% end %>
        </form>



      </div><!--/.navbar-collapse -->
    </div>
  </nav>

  <!-- Main jumbotron for a primary marketing message or call to action -->
  <div class="jumbotron">
    <div class="container">
      <% if user_signed_in? %>
        <h1>Bienvenido a FoodWare <%= current_user.email %> !</h1>
      <% else %>
        <h1>Bienvenido a FoodWare!</h1>
      <% end %>

      <p>Somos la nueva Delivery App que hara tus dias mas felices. Un antojo? Te lo llevamos!</p>
      <% if user_signed_in? %>
        <p><a class="btn btn-info btn-lg" href="/foodplaces" role="button">Ver Locales &raquo;</a></p>
      <% else %>
        <p><a class="btn btn-primary btn-lg" href="/users/sign_up" role="button">Crear Cuenta &raquo;</a></p>
        <p><a class="btn btn-info btn-lg" href="/foodplaces" role="button">Ver Locales &raquo;</a></p>
      <% end %>
    
    </div>
  </div>

  <div class="container">

  <%= yield %>
  

    <hr>

    <footer>
      <p>&copy; 2019 Isa, Marito, Jones.</p>
    </footer>
  </div> <!-- /container -->

  

  </body>
</html>